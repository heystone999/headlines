server:
  port: ${server.port:51701}
spring:
  application:
    name: headlines-schedule
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml