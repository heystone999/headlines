server:
  port: 51806
spring:
  application:
    name: headlines-comment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml